name: AboveFooter Styles
description: AboveFooter styles for the Raiseup theme
type: section

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "#072235"
    text-color:
      type: input.colorpicker
      label: Text
      default: "#d5d5d5"
